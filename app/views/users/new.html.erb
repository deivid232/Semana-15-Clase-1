<%= form_with(model: @user, local: true, multipart: true, class: "form-horizontal") do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset>
    <div class="form-group">
      <%= form.label :name, "Nombre", class: "col-lg-2 control-label" %>
      <div class="col-lg-10">
        <%= form.text_field :name,rows: 10 %>
      </div>
    </div>


    <div class="form-group">
      <%= form.label :email, "Email", class: "col-lg-2 control-label" %>
      <div class="col-lg-10">
        <%= form.text_field :email, rows: 10 %>
      </div>
    </div>

    <div class="form-group">
      <%= form.label :password, "Password", class: "col-lg-2 control-label" %>
      <div class="col-lg-10">
        <%= form.text_field :password, rows: 10 %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-lg-10 col-lg-offset-2">
        <%= form.submit "Enviar", class: "btn btn-primary" %>
        <%= link_to 'Back', histories_path, class: "btn btn-default"  %>
      </div>
    </div>
  </fieldset>
<% end %>
